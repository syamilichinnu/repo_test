<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_label_layout_form_picking" model="ir.ui.view">
        <field name="name">product.label.layout.form</field>
        <field name="model">product.label.layout</field>
        <field name="priority">25</field>
        <field name="inherit_id" ref="product.product_label_layout_form"/>
        <field name="arch" type="xml">
            <!-- Add the boolean field -->
            <xpath expr="//field[@name='custom_quantity']" position="after">
                <field name="is_validate"/>
            </xpath>
            <xpath expr="//field[@name='move_quantity']" position="attributes">
                <attribute name="readonly">is_validate</attribute>
<!--                <attribute name="options">{'no_create': True, 'no_open': True}</attribute>-->
            </xpath>
        </field>
    </record>
    <record id="product_label_layout_form_picking" model="ir.ui.view">
        <field name="name">product.label.layout.form</field>
        <field name="model">product.label.layout</field>
        <field name="priority">25</field>
        <field name="inherit_id" ref="product.product_label_layout_form"/>
        <field name="arch" type="xml">
            <!-- Add the boolean field -->
            <xpath expr="//field[@name='custom_quantity']" position="after">
                <field name="is_validate"/>
            </xpath>
            <xpath expr="//field[@name='move_quantity']" position="attributes">
                <attribute name="readonly">is_validate</attribute>
<!--                <attribute name="options">{'no_create': True, 'no_open': True}</attribute>-->
            </xpath>
        </field>
    </record>
</odoo>